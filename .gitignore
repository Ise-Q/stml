# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/

# Virtual environment
.venv/

# uv
# (uv.lock is committed)

# Notebooks
.ipynb_checkpoints/
# Headless-execution verification artifacts (e.g. eda.executed.ipynb)
*.executed.ipynb

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/

# WSL zone-identifier sidecars
*:Zone.Identifier

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/

# AI Harness
.omc/

# Sreeram_experimental — plan §6
# Raw Bloomberg pulls (large, regenerable from the BBG terminal). Cleaned parquet
# outputs under data/bloomberg/cleaned/ ARE committed.
data/bloomberg/raw/
# The submission deliverables (regenerated by every emit run).
outputs/
# Experiments output (alken pattern — diagnostic, not part of the deliverable).
experiments/results/
