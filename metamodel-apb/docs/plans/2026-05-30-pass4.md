# Pass-4 — feature re-open (F16) + per-instrument embargo + significance-first §6

**PR description / build record.** Competition-cut feature re-open + two correctness items + a
significance-first §6 reporting overhaul. The headline is unchanged and **strengthened**: under more
features and a stricter embargo, IC/AUC are still ≈ 0 — exactly what Grinold's Fundamental Law
predicts. All new behaviour is RED-first TDD with known-answer tests; Ruff strict clean; the three-CSV
deliverable re-emits byte-identical.

## What changed (file-by-file)

| File | Change |
|---|---|
| `cross_validation.py` | **S2.6** per-instrument embargo: `_purge_train` now advances each instrument's `embargo_p90` (trading days) on its own date axis (`_instrument_date_axes`/`_advance_trading_days`), threaded through `PurgedKFold`/`CombinatorialPurgedCV`/`nested_cpcv`; uniform-`pct` path unchanged (gated on `embargo_days is None`). |
| `pipeline.py` | `load_embargo_days()` (reads `instrument_scope.json`), `per_instrument_embargo` + `use_drift` config flags, `instruments` threaded through `select_model`/`fit_oos_calibrator`/`per_instrument_diagnostics`/`nested_cpcv`. |
| `features.py` | **S1.8-b** F16 concept-drift wired into `assemble_instrument_features` (`drift_train_end` gate) via stml's causal `regime_alignment_score`. |
| `signal_analysis.py` | **S5.10** `pesaran_timmermann` (primary), `treynor_mazuy`; `henriksson_merton` relabeled a base-rate-sensitive proxy. |
| `significance.py` (NEW) | **S6.14** `t_statistic`, `ljung_box_test`, `stationary_bootstrap_sharpe_ci` (arch studentised, Politis–White block, Lo-SE studentization, seeded), `sharpe_ci_analytic`, `min_track_record_length`. |
| `sizing.py` | **S6.15** `confidence_taper` (smooth, continuous at the floor), `kappa_baker_mchale`, `cer_improves`; `fractional_kelly`/`position_weight` gain a `taper_width` option. |
| `emit.py` | pass-4 flags on by default (`--no-per-instrument-embargo`, `--no-drift` opt-outs). |
| `experiments/s6_barrier_backtest.py`, `s6_deflation_gate.py`, `x8_feature_counts.py` | pass-4 config; PT/TM + significance + CER-gate + N-ladder; persist net returns. |
| `pyproject.toml` / `uv.lock` | `arch` added. |
| `docs/methodology.md`, `reports/STML_ActionItem_Tracker.md` | §5/§6 rewrite, S6.12/13/14/15, X.7/9/10/11, determinism + limitations; PM-9 Evaluation-Log + status flips. |

## Findings that reshaped the brief (surfaced)

1. **F12/F13/F15 and F17/F3 were already wired** (verified empirically — 13 regime cols incl. `f17_*`; F12/13/15 via `assemble_engineered_ext`), correcting PM-7's premise. The only genuine causal-recompute addition was **F16**. Re-locked count: **Energy 141 / Equity 140 / Metals 141** (+1).
2. **The implemented `henriksson_merton` is a base-rate-sensitive hit-rate proxy**, not canonical H–M — so **Pesaran–Timmermann is the primary §5 timing test**; a base-rate discriminator test pins this.
3. **F17 fit discipline confirmed conforming** (fit ≤ `fe_train_end` prefix; deliverable OOS leak-free), guarded by a test.

## Results (no edge — strengthened)

- AUC ≈ 0.5 (0.568 / 0.536 / 0.532). The two HALT tripwires (expansion clears deflation; §5 audit flips) did **not** fire.
- **§6.14 significance (PRIMARY):** pooled Sharpe 1.31 → **t = 0.93 (n=127, n.s.)**; studentised stationary block-bootstrap 95% CI per-period **[−0.04, +0.19] contains 0**; PSR(0)=0.82; MinTRL=399d; Ljung–Box p=0.010.
- **§6.8 deflation (demoted):** pooled DSR ladder **[0.61 → 0.20]**, PBO 0.35, MinBTL up to 3.1y.
- **§5.10 timing:** PT negative everywhere (pooled −2.31). **Surfaced:** the *pooled* TM γ=+1.18 (t=2.55) is an aggregation artefact (Equity sleeve γ significantly negative) — convex big-move capture from the exit mechanism, not directional skill.
- **§6.15 sizing:** leakage-safe taper CER +5% (immaterial); per-instrument κᵢ variant **circular** (OOS-estimated) → **revert to flat κ=0.25**, deliverable unchanged.

## Verification

- 200 tests pass (+10 RED-first known-answer); Ruff strict clean.
- Byte-identical re-emit verified (two full runs, 4 CSVs identical, canonical == calibrated, 1011 rows).
- Three-file contract: 11 instruments, sorted, ∈[0,1], no NaN.
- No module reads `feature_matrix.parquet` (guard test); F16 truncation-invariant; per-instrument embargo applied; CPCV=15.

## Reproduce

```bash
uv run --directory metamodel-apb pytest -q
uv run --directory metamodel-apb python -m alken_metamodel.emit          # pass-4 deliverable
uv run --directory metamodel-apb python experiments/s6_barrier_backtest.py   # §5/§6 significance + PT/TM + CER
uv run --directory metamodel-apb python experiments/s6_deflation_gate.py     # S6.8 DSR ladder
```

## Out of scope / future work

S6.5 stays the lit-review-default stub. No carry/basis feature (not derivable). A leakage-safe
per-instrument κᵢ (modelling-sample residuals) is the S6.15 follow-up. MLflow not warranted at this
sweep volume (XT.3).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
