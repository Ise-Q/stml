# Plan: T3.03 Alken Metamodel — **Pass 2** (open + exploratory work)

## Context

The pass-1 build is **complete and gradeable** on branch `model/alken-metamodel` (18 commits ahead of main, clean tree): all 8 lit-review commitments implemented, 14 test modules green, both deliverable CSVs validate (11 instruments, 1011 rows each, Jan–Jun 2022, predictions ∈ [0.047, 0.944] no NaN, keys identical, weights 50.9% zero). This pass does **only the open/exploratory work** the action tracker surfaced — it does **not** rebuild anything already `✅`.

**Why now.** A `2026-05-30 (PM)` tracker review flagged that the NN family + CPCV/nested are *implemented but off the default path*, §4 cluster importance was never run on real data (synthetic-only), the backtest uses a simplified holding model missing brief metrics, the §1 macro block is underivable-from-OHLCV but available in `additional_data.xlsx`, and two instruments (ho1s=2, gc1s=30) rest on thin OOS rows. Five exploratory probes (EX.1–EX.5) were opened to characterise *why* the OOS edge is ≈0.5 honestly. **The grade is methodology, not performance** — the goal is to characterise the ≈0.5 edge, not chase it.

**Intent (verifiable end state).** Every open tracker item (S1.7, S3.7, S3.8, S4.7, S5.7, S6.4/S6.7, X.7, XT.2) and all five probes (EX.1–EX.5) completed on **real data**; the byte-deterministic NN family + CPCV-as-selection promoted into the default path; §4 substantiated on the real feature matrix; the §6 backtest made barrier-exact with the full metric set + a cost model; all 11 instruments emit with an honest coverage caveat; a unified `experiment_log.csv`; methodology write-up **and** tracker updated in the same pass; and **all leakage/determinism invariants + both CSV contracts still hold byte-identically on re-run**.

## Verified state (exact signatures already read — do not re-invent)

- `cross_validation.py`: `PurgedKFold(n_splits, t1, pct_embargo)`, `CombinatorialPurgedCV(n_groups, n_test_groups, t1, pct_embargo)` → **C(6,2)=15** and **sklearn-cv-compatible** (`.split(X)` + `.get_n_splits()`), `nested_cpcv(X, t1, *, outer_groups, outer_test_groups, inner_groups, inner_test_groups, pct_embargo)` generator → `(outer_train, outer_test, inner_cv)`.
- `pipeline.select_model(X, y, t1, sample_weight, config)` builds `cv = PurgedKFold(n_splits, t1, pct_embargo)` then `cross_val_evaluate(...)`. → **S3.8 is a one-line `cv` swap gated by config**, not a rewrite.
- `cluster_importance.cluster_feature_importance(X, y, t1, *, seed, n_splits, pct_embargo, max_clusters)` → `(table[mdi/mda/shap], clusters)` — **already real-data ready; just needs running.** Bug-fixes confirmed in `_vendor/cluster_feature_importance.py` (Mantegna `√(1−|ρ|)` @ line 34; injected `cv_splitter` @ `calculate_cluster_importance_pfi` line 107).
- `neural.py`: `TorchMLP`, `TorchVSN` (byte-deterministic), `KerasVSN` (**TF non-byte-deterministic** — off-path only), `full_roster`. VSN builds **one GRN per feature**, input dim = `x.shape[1]` → EX.2 reduction needed for tractability.
- `backtest.performance_metrics` → `{n,total_return,cagr,ann_vol,sharpe,sortino,max_drawdown}`. **Sortino already exists.** No turnover / avg-holding / transaction-cost / barrier-exact logic yet.
- `features.assemble_instrument_features` → core (`assemble_engineered`) + ext + 24 z-twins + 3 trend ≈ **~64+ per instrument** (NOT 124 — *measure and write the real pooled count*; pooled class panel adds regime + instrument one-hots).
- `additional_data.xlsx`: single sheet, 1990→2022-06-30 business-daily, **all wish-list series present** (VIX/VIX3M, MOVE, DXY, CBOE_SKEW, 2Y/10Y_UST, TIPS10Y, BE10Y, EIA_CRUDE/DIST/GASOLINE/NG_STOCK, HY_OAS, IG_OAS, CHINA_PMI_MFG, US_ISM_MFG_PMI, LME_COPPER_STOCK, …) but **observation dates only — no release-date columns** → PIT-lag must be encoded from publication calendars.
- Skills present: `python-quant-finance` (quantstats/vectorbt, Kelly), `aqms-python` (`aqms_quant.py`, L4 IR=IC√BR + Grinold–Kahn costs + turnover), `empirical-finance` (`performance_metrics.py` Sortino, CER, bootstrap CIs), `bdfin-ml` (`unsupervised.md` PCA/clustering, `calibration.py`, `shap_helpers.py`; **no autoencoder module**), `sts-ml` (canonical scripts).
- **LR.5 is an unrun tracked task** — the `[NOTE FOR WRITEUP LEAD]` flags (Ang–Bekaert correlation values; Carver 2015 pages) live inline in `nlr-cw-v1.md`. X.7 stays flagged; **do not invent values.**

## Hard rules (re-assert, do not violate)

Writes confined to project root; released data + frozen `feature_matrix.parquet` read-only/untouched (parquet never consumed by the pipeline). Zero train/val `t1`-span overlap; embargo `⌈0.01·T⌉`; CPCV = 15 paths; right-edge truncation-invariance; scaler/estimator **train-only** (and EX.2 reducer basis fit on the locked modelling sample only); lags shifted; seeds module-wide via `seeding.set_seeds` (single-thread kernels). **Both CSVs re-emit byte-identical.** Feature set + label hyperparameters **locked before final-OOS** — no snooping Jan–Jun 2022; hidden Jul–Dec 2022 untouched. uv only; Ruff strict (verify `--no-fix` — global `fix=true`). New behaviour is TDD RED→GREEN with mutation-resistant assertions. **S6.5 stays a marked stub** (Q2 constraints doc absent).

## Decisions (user-confirmed this session)

1. **Default path = torch-only + Keras off-path.** Promote `tree_linear + torch_mlp + torch_vsn` (byte-deterministic) + CPCV→selection into the shipped default. **KerasVSN stays off-path** as a documented §3 comparison (TF non-determinism would break the grader's hidden-half re-run). Deliverable CSVs get re-emitted (may differ from pass-1) but **byte-identical on re-run** — verifier asserts twice.
2. **EX.2 = build & compare all three** (cluster-rep, PCA, torch autoencoder); **promote cluster-representative selection** as the reducer feeding S3.7 (deterministic, interpretable, ties to §4 Mantegna clusters).
3. **Q3 confirmed** — backtest metrics + cost model pull from `/python-quant-finance` + `/aqms-python` + `/empirical-finance`.
4. **EX.1 + EX.3 are diagnostic-only** — the (k, T_max) surface and label-horizon probe characterise on the **modelling sample**; results are reported descriptively and **must not feed back into the locked `pt_sl`/`max_holding`** (else they snoop the rehearsal half).
5. **XT.3 (MLflow): defer** — `experiment_log.csv` (XT.2) is sufficient; recommend MLflow only if a later sweep explodes run count.

## Dependency spine (sequential — build before fan-out; TDD RED→GREEN each)

| Step | File(s) | One-line change | RED-first tests |
|---|---|---|---|
| **EX.2** | **new** `dim_reduction.py` | `ClusterRepSelector` (one feature per Mantegna §4 cluster via `make_clusters`), `PCAReducer`, `AutoencoderReducer` (small torch); common `fit(train)/transform` API | reducer fit train-only (no leakage); reduced dim < full; cluster-rep picks **exactly one** feature per cluster; autoencoder deterministic |
| **S1.7** | **new** `macro.py` | PIT-lagged loader over `additional_data.xlsx`: per-series publication-lag calendar (EIA weekly +3–7d, PMI +1 bday/next month, daily market series same-day), reindex to trade dates, **shift by lag**; emits VIX term-slope, EIA inventory Δ, TIPS/BE10Y, DXY, MOVE, HY/IG OAS, CHINA_PMI | each macro feature at `t` uses only data **released ≤ t** (PIT assertion); truncation-invariant; never reads beyond `predict_end` |
| **S3.8** | edit `pipeline.py`, `PipelineConfig` | add `cv_scheme: str = "purged"` → `select_model` swaps `PurgedKFold`↔`CombinatorialPurgedCV`; nested-CPCV path for the headline model only; **CPCV smoke-test on real Energy first** (15 paths → small folds → single-class NaN AUC risk; confirm `nanmean` selection doesn't degenerate to 1–2 finite paths) | selection uses CPCV when configured (15 paths); nested inner∩outer-val = ∅; degenerate-fold guard |
| **S3.7** | edit `neural.py` (+`models.py`), `pipeline.py` | new `default_roster = tree_linear + torch_mlp + torch_vsn`; wire EX.2 cluster-rep reduction into the NN feature path; re-run Equity/Energy/Metals selection with NN included; `roster="default"` becomes the shipped config | `default_roster` excludes KerasVSN; reduced-feature NN fits; deterministic refit |

## Dynamic-Workflow fan-out (after the spine — `≤16 concurrent`, run via Workflow on approval)

Reproducible probe scripts under **new** `metamodel-apb/experiments/` (each writes a finding + a row to `experiment_log.csv`), parallelised:

- **EX.1** `experiments/ex1_edge_decomposition.py` — decompose ≈0.5 edge into primary-signal quality / label-horizon / feature-relevance; characterise Equity 15/15 vs Energy 6/15 CPCV-paths>0.5. **Diagnostic-only.**
- **EX.3** `experiments/ex3_barrier_surface.py` — (k, T_max) grid → label distribution + **modelling-sample** AUC per cell (embarrassingly parallel). **Diagnostic-only; no feedback to locked config.**
- **EX.4** `experiments/ex4_calibration.py` (+ **new** `calibration.py`) — reliability diagrams + Platt/isotonic per selected model; tie to the AUC≠P&L finding (Kelly consumes p̂). Tests: perfectly-calibrated input → diagonal; isotonic monotone.
- **EX.5** `experiments/ex5_signal_characterisation.py` (+ **new** `signal_analysis.py`) — primary-signal turnover, hit-rate, regime-conditional perf, IR=IC√BR (`/aqms-python`). Tests: known-value hit-rate/turnover.
- **S4.7** `experiments/s4_cluster_importance.py` — run `cluster_feature_importance` on the **real** pooled matrix, **Energy first → Equity/Metals** (3 streams); report real cluster groups, which drive the metamodel, confirm noise-clusters≈0. Module reused unchanged.
- **S6.7 / S6.4** edit `backtest.py` (+ **new** `cost_model.py`) — barrier-exact exit (on actual `t1` touch, not `max_holding`) with position netting for overlapping labels; add **turnover, avg holding period, transaction costs** (spread + Grinold–Kahn); extend `performance_metrics`. Tests: known-value turnover; cost monotonic in size; barrier-exact exit matches hand-built path; netting nets offsetting positions.
- **S5.7** edit `emit.py` / write-up — emit all 11 (no abstention) **with a per-instrument OOS-coverage caveat**; flag ho1s (2 rows), gc1s (30); surface §5 AUCs on thin training counts (ho1s/ng1s 61/68) as unreliable. Tests: caveat table lists per-instrument OOS row counts.

## Cross-cutting

- **XT.2** — **new** `experiment_log.py` (or helper in `evaluation.py`): one row/run `{run_id, config, cv_scheme, roster, reducer, asset_class, oos_auc, brier, …}` appended deterministically across spine + fan-out.
- **X.7** — leave the `[NOTE FOR WRITEUP LEAD]` flags in place (LR.5 unrun); list them for the write-up lead; **do not invent** Ang–Bekaert / Carver values.
- **XT.3** — recommend deferring MLflow; note the trigger condition in the write-up.

## Invariant & adversarial verifier stream (read-only, after changes)

Independently re-check: zero train/val `t1` overlap; embargo `⌈0.01·T⌉`; CPCV = 15; right-edge truncation-invariance; reducer/scaler train-only; **both bug-fixes intact** (Mantegna distance line; injected PurgedKFold MDA); **both CSV contracts** (schema `date,instrument,prediction` / `…,weight`; 11 instruments; sorted `(date,instrument)`; key parity; ∈[0,1]; no NaN) and **byte-identical on double re-emit**. **Any failure → halt and surface the diff**; do not auto-resolve and continue.

## Files (summary)

- **New modules:** `dim_reduction.py`, `macro.py`, `cost_model.py`, `calibration.py`, `signal_analysis.py`, `experiment_log.py` (+ mirrored `tests/test_*.py`, RED-first).
- **New scripts:** `experiments/{ex1,ex3,ex4,ex5,s4_cluster_importance}.py`.
- **Edited:** `pipeline.py` (cv_scheme, default roster, nested headline), `neural.py`/`models.py` (default_roster), `backtest.py` (barrier-exact + metrics + costs), `emit.py` (re-emit + coverage caveat), `docs/methodology.md` (§1/§3/§4/§5/§6 + Limitations), `reports/T3.03_STML_Action_Tracker.md` (dated Evaluation-Log entry + status flips per its update protocol).
- **New outputs:** re-emitted `metamodel_predictions.csv` + `strategy_weights.csv`; `experiment_log.csv`.
- **Plan committed to** `metamodel-apb/docs/plans/2026-05-30-pass2.md` (PR description). **No PR drafted unless asked.**

## Verification (Definition of Done)

1. **Tests:** `uv run pytest` green; every new module RED-first with mutation-resistant known-value assertions; `uv run ruff check --no-fix` clean.
2. **Determinism:** double re-emit byte-identical (torch-only default); flip window config → different rows (no hardcoded Jan–Jun).
3. **Leakage invariants** re-asserted by the verifier stream (list above); PIT-lag + reducer-train-only proven by test.
4. **Coverage:** all open tracker items + EX.1–EX.5 completed on real data; S6.5 remains a marked stub; §4 on the real matrix; §6 barrier-exact + turnover/avg-holding/costs; NN(torch) + CPCV in default; all 11 emit with caveat.
5. **Docs synced:** `methodology.md` §1/§3/§4/§5/§6 + Limitations updated with real numbers + the EX findings; **new dated tracker Evaluation-Log entry + status flips**; `experiment_log.csv` exists; X.7 flags listed (not invented).

## Edge cases / risks

- **CPCV small-fold degeneracy** — 15 paths → single-class test folds → NaN AUC; smoke-test on Energy before S3.8 ships; guard selection against degenerating to 1–2 finite paths.
- **Autoencoder over-fit at small N** — built for comparison only; cluster-rep is the promoted reducer.
- **Real pooled feature count** — measure and write the number; do not assert "124".
- **EX.3/EX.1 snooping** — diagnostic-only firewall enforced (no feedback to locked config).
- **TF determinism** — KerasVSN kept off the selectable path; reported with a non-bit-reproducibility caveat.
